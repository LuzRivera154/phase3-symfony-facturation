<div{{attributes}}
	class="bg-blue-100 flex flex-col h-full py-5 w-1/2 md:w-64 shrink-0">
	<!-- component HTML -->
	<div class="flex items-center justify-center gap-2 py-6 mb-5">
		<img src="{{ asset('imagenes/Icon.png') }}" alt="icon.png">
		<h2 class="link-color-primary font-bold pt-2 text-2xl">FacturSaaS</h2>
	</div>
	<div class="flex flex-col text-lg gap-1 flex-1 min-h-0 overflow-y-auto">
		<a href="{{path('app_dashboard')}}" class="links-nav">
			<i class="fa-solid fa-table"></i>
			Dashboard
		</a>
		<a href="" class="links-nav">
			<i class="fa-solid fa-file-lines"></i>
			Factures
		</a>
		<a href="" class="links-nav">
			<i class="fa-solid fa-user-group"></i>
			Clients
		</a>
		<a href="" class="links-nav">
			<i class="fa-solid fa-box"></i>
			Produits
		</a>
		<a href="{{path('app_profile')}}" class="links-nav">
			<i class="fa-solid fa-gear"></i>
			Profile
		</a>
	</div>
	<div class="flex flex-col gap-5">
		<div class="flex gap-2 px-2">
			<div class="bg-blue-200 rounded-full p-4 text-blue-700 font-extrabold max-w-16 text-center ">
				{{ this.user.firstName|first|upper }}{{ this.user.lastName|first|upper }}

			</div>
			<div class="flex flex-col">
				<p>{{ this.user.companyName|capitalize }}</p>
				<p class="text-gray-500">{{ this.user.email }}</p>
			</div>
		</div>
		<a href="{{path('app_logout')}}" class="text-red-800 px-5 flex gap-3 text-base items-center hover:text-red-500">
			<i class="fa-solid fa-right-from-bracket"></i>
			Déconnexion</a>
	</div>
</div>
